using System.Windows;

namespace ProcAI.App;

/// <summary>
/// WPF application entry point. The full Fluent shell (sidebar + 10 pages, tray,
/// notifications, consent flow) is built in the GUI phase; this is the minimal
/// bootstrap so the project compiles and runs.
/// </summary>
public partial class App : Application
{
}
