<Page x:Class="ProcAI.App.Views.AlertsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Alerts" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Opacity="0.7" />
            <ComboBox x:Name="FilterBox" Width="160" Margin="8,0" SelectionChanged="OnFilterChanged">
                <ComboBoxItem Content="All" IsSelected="True" />
                <ComboBoxItem Content="Critical" />
                <ComboBoxItem Content="High" />
                <ComboBoxItem Content="Medium" />
                <ComboBoxItem Content="Low" />
                <ComboBoxItem Content="Unacknowledged" />
            </ComboBox>
            <ui:Button Content="Acknowledge" Margin="4,0" Click="OnAcknowledge" />
            <ui:Button Content="Add to allowlist" Margin="4,0" Click="OnAllowlist" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" Background="#1A222C" RowBackground="#1A222C"
                      AlternatingRowBackground="#202A36" BorderThickness="0" Margin="0,0,8,0"
                      SelectionChanged="OnSelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="140" />
                    <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90" />
                    <DataGridTextColumn Header="Process" Binding="{Binding Name}" Width="160" />
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="60" />
                    <DataGridTextColumn Header="Risk" Binding="{Binding Risk}" Width="55" />
                    <DataGridTextColumn Header="Top reason" Binding="{Binding Reason}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Background="#1A222C" CornerRadius="12" Padding="14">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Alert Detail" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBox x:Name="Detail" DockPanel.Dock="Top" IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" Background="#222C38" BorderThickness="0"
                             Text="Select an alert to see details." />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
