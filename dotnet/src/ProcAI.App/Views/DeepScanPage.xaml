<Page x:Class="ProcAI.App.Views.DeepScanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Deep Scan" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <ui:Button x:Name="ScanButton" Content="Run Deep Scan" Appearance="Primary" Click="OnRun" />
            <ui:Button Content="Run on simulation data" Margin="8,0" Click="OnRunSim" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="8,0" Opacity="0.7" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="Findings">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#222C38" CornerRadius="8" Padding="10" Margin="0,3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{Binding SeverityBrush}" CornerRadius="6"
                                        Padding="8,2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Severity}" Foreground="#0F1419" FontSize="11" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="{Binding Detail}" Margin="10,0" VerticalAlignment="Center"
                                           TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
