<Page x:Class="ProcAI.App.Views.TimelinePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Forensic Timeline" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Window:" VerticalAlignment="Center" Opacity="0.7" />
            <ComboBox x:Name="WindowBox" Width="140" Margin="8,0" SelectionChanged="OnWindowChanged">
                <ComboBoxItem Content="Last hour" />
                <ComboBoxItem Content="Last 24h" IsSelected="True" />
                <ComboBoxItem Content="Last 7 days" />
            </ComboBox>
            <ui:Button Content="Refresh" Click="OnRefresh" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" Background="#1A222C" RowBackground="#1A222C"
                  AlternatingRowBackground="#202A36" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="160" />
                <DataGridTextColumn Header="Event" Binding="{Binding Type}" Width="110" />
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90" />
                <DataGridTextColumn Header="Process" Binding="{Binding Process}" Width="200" />
                <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
