<Page x:Class="ProcAI.App.Views.AssistantPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Proc Assistant" FontSize="24" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" x:Name="ModeLabel" Opacity="0.7" FontSize="12" Margin="0,0,0,8" />

        <TextBox Grid.Row="2" x:Name="Chat" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" Background="#1A222C" BorderThickness="0" Padding="12" />

        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" x:Name="Input" PlaceholderText="Ask about an alert, a PID, or your protection status..." />
            <ui:Button Grid.Column="1" Content="Send" Margin="8,0,0,0" Click="OnSend" />
            <ui:Button Grid.Column="2" Content="Explain latest alert" Margin="8,0,0,0" Click="OnExplainLatest" />
        </Grid>
    </Grid>
</Page>
