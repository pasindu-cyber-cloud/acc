# ProcAI runtime requirements (full feature set).
# Core detection engine only strictly requires psutil; the rest enable ML, GUI,
# reporting and the optional AI assistant.

# --- Core ---
psutil>=5.9

# --- Machine learning (Decision Tree / Random Forest) ---
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3

# --- GUI + system tray ---
customtkinter>=5.2
pystray>=0.19
Pillow>=10.0

# --- Reports (PDF) ---
reportlab>=4.0

# --- Optional AI assistant (Gemini REST / Ollama local) ---
requests>=2.31
