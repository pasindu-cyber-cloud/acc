"""ProcAI graphical interface (CustomTkinter).

The GUI is an optional extra. Importing this package does not require
customtkinter; the heavy import happens inside :func:`procai.gui.app.run` so the
core engine and the headless service remain dependency-light.
"""
